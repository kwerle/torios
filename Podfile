# Uncomment this line to define a global platform for your project
platform :ios, '8.0'
use_frameworks!

target 'torios' do
  pod 'Alamofire'
  pod 'Locksmith'
  pod 'SwiftyJSON', :git => 'https://github.com/SwiftyJSON/SwiftyJSON.git'
end

target 'toriosTests' do

end

target 'toriosUITests' do

end
